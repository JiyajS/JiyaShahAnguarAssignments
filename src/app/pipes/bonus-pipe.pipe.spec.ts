import { BonusPipePipe } from './bonus-pipe.pipe';

describe('BonusPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BonusPipePipe();
    expect(pipe).toBeTruthy();
  });
});
